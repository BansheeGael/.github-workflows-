// Сначала блок plugins
plugins {
    id 'java'
    id 'fabric-loom' version '1.0-SNAPSHOT'
}

// Затем репозитории
repositories {
    mavenCentral()
    maven { url 'https://maven.fabricmc.net/' }
    maven { url 'https://maven.minecraftforge.net/' }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

dependencies {
    // Ваши зависимости
}
